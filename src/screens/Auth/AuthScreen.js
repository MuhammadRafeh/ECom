import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const Auth = props => {
    return (
        <View />
    );
}

export default Auth;

const styles = StyleSheet.create({
    
})
