import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const RateAppScreen = (props: any) => {
    return (
        <View />
    );
}

export default RateAppScreen;

const styles = StyleSheet.create({

})
